[package]
name = "cf-gateway-sync"
version = "0.1.0"
edition = "2021"

[dependencies]
ureq = { version = "2.9", features = ["json"] }
serde_json = "1.0"

[profile.release]
lto = true
codegen-units = 1
strip = true
